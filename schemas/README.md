# Schemas

## Future Schemas

Schemas may be added when canonical objects or interfaces require machine-readable validation.

## Current Authority

During v0.1, this directory is documentation-only unless repository-specific schemas are explicitly required.

## Versioning

Schema files should include a version field or filename version when introduced.

## Runtime Schemas

Do not invent runtime schemas during v0.1 unless explicitly required by the repository-specific baseline.

